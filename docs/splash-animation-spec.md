# 星云编年史 · 启动动画需求规格书

> 版本：v1.1  
> 日期：2026-06-24  
> 状态：待审查  
> 用途：初赛作品启动页 / 开机动画

---

## 1. 项目背景

《星云编年史》目前拥有 8 张历届世萌萌王角色海报，已用于登录页背景轮播。为了提升作品初亮相时的仪式感与视觉冲击力，用户希望在进入登录页/主应用之前，先播放一段启动动画：8 位角色以 **liquid-glass-demo.html 同款琉璃玻璃卡片** 的形式呈现，卡片会整体旋转、分散、聚合，类似高端手机或游戏的开机动画，最终浮现作品名，再进入正式页面。

---

## 2. 需求目标

- 制作一段 8~10 秒的启动动画（Splash / Boot Animation）。
- 8 位角色分别呈现在「动态琉璃玻璃卡片」中，视觉风格与 `/workspace/liquid-glass-demo.html` 的玻璃态系统保持一致。
- 玻璃卡片会整体旋转（轮播）、分散、聚合，类似高端手机或游戏的开机动画。
- 动画结尾浮现作品名《星云编年史 NEBULA CHRONICLE》。
- 动画结束后进入登录页 `/login.html`（或主应用 `/nebula-chronicle.html`，可配置）。
- 启动动画基于现有 `/workspace/liquid-glass-demo.html` 进行扩展，复用其深空背景、动态光斑、玻璃卡片质感、高光边框与鼠标跟随高光。
- 保持初赛阶段改动量可控，不重构整个项目架构。

---

## 3. 动画分镜脚本（时间轴）

| 阶段 | 时间 | 画面描述 | 音效预留 |
|---|---|---|---|
| **Phase 0: 暗场** | 0.0s ~ 0.5s | 深空黑背景，中央出现微弱光点 | 无 / 低频嗡鸣起势 |
| **Phase 1: 卡片诞生** | 0.5s ~ 1.5s | 光点凝聚成一张半透明琉璃玻璃卡片，内部显现第一位角色 | 玻璃凝结音效 |
| **Phase 2: 分裂扩散** | 1.5s ~ 3.5s | 卡片分裂为 8 张较小的玻璃卡片，向四周散开，排列成三维圆环/球面阵列；每张卡片缓慢自转并绕中心公转 | 清脆的分裂+扩散音 |
| **Phase 3: 环绕展示** | 3.5s ~ 5.5s | 8 张玻璃卡片持续缓慢公转，卡片自身也微微倾斜旋转；角色图像清晰可见，每张卡片边缘有对应主题色微光 | 轻柔环境音 |
| **Phase 4: 聚合融合** | 5.5s ~ 6.5s | 8 张玻璃卡片向中心聚合，叠合为一个发光的巨大玻璃卡片/玻璃面板 | 聚合/能量汇聚音效 |
| **Phase 5: 爆裂显字** | 6.5s ~ 7.5s | 玻璃卡片爆散成光粒，光粒中浮现「星云编年史」中文标题与「NEBULA CHRONICLE」英文副标题 | 爆裂+标题显现音效 |
| **Phase 6: 入口就绪** | 7.5s ~ 10.0s | 标题稳定显示，下方出现「进入」按钮或提示语；用户点击/按键后进入登录页 | 结束提示音 |
| **Phase 7: 过渡离场** | 10.0s+ | 标题淡出，登录页淡入；或直接进入登录页 | 无 |

---

## 4. 视觉设计规范

### 4.1 玻璃卡片外观（复用 liquid-glass-demo 风格）

- **材质**：半透明液态玻璃（Liquid Glass），与 `liquid-glass-demo.html` 的 `.glass-card` 一致。
- **形状**：默认圆角矩形卡片（圆角 24~36px），可备选圆角正方形或竖版海报比例。
- **光学效果**：
  - `backdrop-filter: blur(28px)` 磨砂玻璃质感
  - 顶部轮廓高光（白色渐变边框，mask-composite 实现）
  - 玻璃表面鼠标跟随高光（`.sheen`）
  - 卡片边缘带角色主题色辉光（box-shadow）
  - 多层深度：后排有装饰玻璃层 `.glass-back`
- **内部内容**：角色海报以图片形式置于玻璃卡片中央，图片本身可再覆盖一层微弱玻璃质感，使其与卡片融合。

### 4.2 动效风格

- **整体运动**：平滑、带弹性缓冲（ease-out / spring），避免生硬线性。
- **旋转**：
  - 公转：8 张卡片围绕中心 Y 轴缓慢旋转（约 8~12 秒/圈）。
  - 自转：每张卡片自身有轻微 3D 倾斜（rotateY / rotateX），增强立体感。
- **聚散动画**：
  - 扩散时：从中心向外呈爆炸式弧线运动，带轻微延迟错落（stagger）。
  - 聚合时：从四周向中心加速回收，最后轻微过冲再稳定。

### 4.3 色彩与氛围

- **背景**：深空黑 `#050714`，复用 `liquid-glass-demo.html`。
- **动态光斑**：复用 `.orb` 蓝紫青三色光斑，缓慢漂浮。
- **玻璃高光**：冷白 `#ffffff` + 角色主题色。
- **标题文字**：
  - 中文「星云编年史」：大字、无衬线、白色、轻微发光。
  - 英文「NEBULA CHRONICLE」：小字、科技感字体、霓虹青蓝 `#00f3ff`。

### 4.4 字体

- 中文标题：Noto Sans SC / 思源黑体 Bold
- 英文副标题：Inter / Poppins / Space Grotesk

---

## 5. 技术实现方案

### 5.1 方案 A：Three.js WebGL 3D 卡片（推荐）

- 使用 Three.js 创建 3D 场景。
- 每张玻璃卡片用 `MeshPhysicalMaterial` 或 `ShaderMaterial` 模拟透光/清漆高光/折射。
- 卡片贴图：8 张角色海报。
- 使用 `PointLight` + `AmbientLight` 营造玻璃质感。
- 动画使用 GSAP 或 Tween.js 控制位置、旋转、缩放。
- 最后阶段用 CSS 覆盖层显示标题。
- **优点**：效果最炫，3D 纵深真实，玻璃折射自然。  
- **缺点**：需要加载 Three.js，低端设备可能帧率下降。

### 5.2 方案 B：纯 CSS 3D 卡片轮播（轻量备选）

- 使用 CSS `transform-style: preserve-3d` + `rotateY/rotateX` + `translateZ`。
- 玻璃卡片复用 `liquid-glass-demo.html` 的 `.glass-card` 样式。
- 图片用卡片内的 `img` 或 `background-image`。
- 动画用 CSS `@keyframes` 控制公转、自转、聚散。
- **优点**：轻量、无需 WebGL、加载快、与现有 glass-card 样式 100% 一致。  
- **缺点**：3D 纵深和玻璃折射弱于 Three.js。

### 5.3 最终推荐

- **初赛阶段采用方案 A（Three.js）**，因为用户明确要求「转圈」「分开」「聚合」的 3D 空间运动，Three.js 更容易做出卡片在三维空间中的弧线聚散。
- 同时保留 `liquid-glass-demo.html` 的玻璃态视觉语言（背景光斑、卡片高光、边框样式），让启动动画与该页面无缝融合。
- 提供「跳过动画」按钮与 `prefers-reduced-motion` 媒体查询降级。

---

## 6. 页面结构

> 说明：启动动画将基于 `/workspace/liquid-glass-demo.html` 的视觉系统进行扩展，复用其深空背景、动态光斑、玻璃卡片质感与鼠标跟随高光。

### 6.1 方案一：直接改造现有页面（推荐）

```
/workspace/liquid-glass-demo.html（可重命名为 splash.html 或 index.html）
├── 动态光斑背景层（复用现有 .ambient-light）
├── 全屏 WebGL Canvas（新增：8 张琉璃玻璃卡片动画）
├── 标题覆盖层（新增，动画结束后显示）
│   ├── 星云编年史
│   ├── NEBULA CHRONICLE
│   └── 进入 / Enter 按钮
├── 玻璃卡片登录框（保留现有 .glass-card，动画结束后滑入）
├── 跳过按钮（右上角）
└── 加载提示（首次访问显示）
```

### 6.2 方案二：新建启动页，复用视觉系统

```
/workspace/splash.html
├── 复用 liquid-glass-demo.html 的背景与玻璃材质 CSS
├── 全屏 WebGL Canvas（琉璃玻璃卡片动画）
├── 标题覆盖层
├── 跳过按钮
└── 动画结束后跳转至 /login.html
```

**推荐方案一**：将 `liquid-glass-demo.html` 直接升级为「启动动画 + 登录入口」合一页面，初赛阶段改动最小，且视觉风格完全统一。

---

## 7. 素材清单

| 素材 | 当前状态 | 说明 |
|---|---|---|
| 8 张角色海报 | 已存在 `/workspace/images/login/` | 直接复用，作为玻璃卡片贴图/内容 |
| 标题字体 | 待引入 | Noto Sans SC + Space Grotesk |
| Three.js | 待引入 CDN | r128 或更新版本 |
| GSAP | 待引入 CDN | 用于动画编排 |
| 环境贴图（可选） | 待生成 | 用于增强玻璃反射 |
| 音效（可选） | 预留接口 | 初赛可先静音 |

---

## 8. 交互逻辑

1. 用户打开 `liquid-glass-demo.html`（或重命名后的启动页）。
2. 页面预加载 Three.js 与角色贴图。
3. 加载完成后自动播放动画。
4. 动画全程可点击「跳过」直接进入登录页/显示登录卡片。
5. Phase 6 显示标题与「进入」按钮，等待用户交互。
6. 用户点击/按空格/回车后，淡出并跳转至 `/login.html`（或本页登录卡片滑入）。
7. 增加 `localStorage` 标记：`hasSeenSplash=true`，下次访问可选择直接跳过（可选）。

---

## 9. 性能与兼容性

- **目标帧率**：桌面 60fps，移动端 30fps+。
- **降级策略**：
  - WebGL 不支持时，降级为静态标题页或 CSS 简化版。
  - 低性能设备减少卡片数量、关闭折射、降低分辨率。
- **无障碍**：
  - 支持 `prefers-reduced-motion` 关闭动画。
  - 跳过按钮始终可聚焦。

---

## 10. 待确认事项

请在审查后确认或修改以下内容：

1. **页面选择**：直接改造 `liquid-glass-demo.html`（动画+登录合一），还是新建 `splash.html`（动画后跳转登录页）？
2. **动画时长**：建议 8~10 秒，是否需要缩短或延长？
3. **卡片形状**：优先竖版圆角矩形（类似海报），是否接受正方形或圆角立方体？
4. **角色排列**：扩散后是排成圆环（2D 平面轮播）还是球面（3D 空间）？
5. **结束去向**：动画结束后直接进入登录页 `/login.html`，还是在本页显示登录卡片？
6. **是否自动跳转**：Phase 6 后是否等待用户点击，还是 2 秒后自动进入？
7. **跳过机制**：是否支持「首次播放后不再显示」？
8. **音效**：初赛是否先不加音效，仅做视觉？
9. **技术方案**：是否同意使用 Three.js 实现？

---

## 11. 下一步计划

用户确认本规格书后：

1. 基于 `/workspace/liquid-glass-demo.html` 扩展启动动画，保留其背景光斑与玻璃卡片系统。
2. 引入 Three.js + GSAP（CDN）。
3. 实现 8 张琉璃玻璃卡片模型，贴入角色海报。
4. 编排聚散旋转动画。
5. 动画结束后显示标题与登录卡片（或跳转至 `/login.html`）。
6. 本地预览并截图/录屏给用户确认。
7. 确认后合并到仓库。

---

## 附录：参考效果描述

- **类似效果**：高端手机开机动画、PlayStation/Xbox 开机粒子聚合、二游启动器角色环绕、Apple 液态玻璃材质展示。
- **核心情绪**：神秘 → 绽放 → 聚合 → 爆发 → 庄严。
